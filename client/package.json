{
    "name": "my-project-client",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "npx nodemon --exec 'NODE_ENV=development vite build -l error --watch --mode=development' --watch vite.config.js",
        "build": "vite build",
        "preview": "vite preview"
    },
    "dependencies": {
        "ackee-tracker": "^5.1.0",
        "react": "^17.0.2",
        "react-dom": "^17.0.2"
    },
    "devDependencies": {
        "@vitejs/plugin-react": "^1.0.0",
        "vite": "^2.6.4"
    }
}
